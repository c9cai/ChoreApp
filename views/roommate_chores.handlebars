<!DOCTYPE html>
<html lang="en">
<head>
    <title>Chore Hero</title>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link href="css/home.css" rel="stylesheet">
    <link href="css/roommate_chores.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="slick/slick.css"/>
    <link rel="stylesheet" type="text/css" href="slick/slick-theme.css"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>

<nav class="navbar navbar-inverse">
    <div class="row">
        <div class="col-xs-2 col-md-1">
            <div class="navbar-header">
                <button id="hamburger" style="font-size: 250%;" class="btn-transparent btn-default" type="button" onclick="toggleNav()">
                    <span class="glyphicon glyphicon-menu-hamburger"></span>
                </button>
            </div>
        </div>
        <div class="col-xs-3 col-md-6">
            <h1 id="app-name">Chore Hero</h1>
        </div>
        <div class="col-xs-3 col-md-1 pull-right">
            <div class="well" id="score-label">
                {{rating}}
            </div>
        </div>
        <div class="col-xs-4 col-md-4 pull-right">
            <div class="progress">
                <div class="progress-bar" id="score-bar" role="progressbar" aria-valuenow="{{rating}}"
              aria-valuemin="0" aria-valuemax="100" style="width:{{rating}}%">
                </div>
            </div>
        </div>
    </div>
</nav>

<div id="side-nav" class="sidenav">
    <div class="row">
        <h4>Hi, {{firstName}}</h4>
        <a href="javascript:void(0)" class="closebtn" onclick="toggleNav()">&times;</a>
    </div>
    <a href="/home"><span class="glyphicon glyphicon-home"></span>&nbsp; My Chores</a>
    <a href="/roommate_chores"><span class="glyphicon glyphicon-user"></span>&nbsp; Roommates' Chores</a>
    <a href="/scores"><span class="glyphicon glyphicon-stats"></span>&nbsp; Scores</a>
    <a href="/preferences"><span class="glyphicon glyphicon-heart"></span>&nbsp; Preferences</a>
    <a href="/settings"><span class="glyphicon glyphicon-wrench"></span>&nbsp; Settings</a>
    <div class="divider"></div>
    <form id="logout" method="post" action="/logout">
        <button type="submit" class="btn btn-default" id="logout-button" form="logout">Log Out</button>
    </form>
</div>




<div class="container-fluid text-center">
    <h1 class="text-center">Roommates' Chores</h1>
    <div class="row">
        <div class="col-xs-12 col-md-10 col-md-offset-1"><hr>
        </div>
    </div>
    <br/>
    <div class="container-fluid">
        <h4 class="time-titles overdue-title">Overdue</h4>
        <div class="row">
            <div class="col-md-8 col-md-offset-2 col-xs-12 carousel-container">
                <div class="carousel">
                    {{#each users}}
                        {{#each overdue}}
                        <div>
                            <button disabled type="button" class="btn btn-lg chore-buttons overdue-buttons" data-toggle="modal" data-target="#myModal">
                                {{chorename}}</br>{{duedate}}</br>({{../firstName}})
                            </button>
                        </div>
                        {{/each}}
                    {{/each}}
                </div>
            </div>
        </div>

        <h4 class="time-titles">Today</h4>
        <div class="row">
            <div class="col-md-8 col-md-offset-2 col-xs-12 carousel-container">
                <div class="carousel">
                    {{#each users}}
                        {{#each today}}
                        <div>
                            <button disabled type="button" class="btn btn-lg chore-buttons" data-toggle="modal" data-target="#myModal">
                                {{chorename}}</br>{{duedate}}</br>({{../firstName}})
                            </button>
                        </div>
                        {{/each}}
                    {{/each}}
                </div>
            </div>
        </div>

        <h4 class="time-titles">Upcoming</h4>
        <div class="row">
            <div class="col-md-8 col-md-offset-2 col-xs-12 carousel-container">
                <div class="carousel">
                    {{#each users}}
                        {{#each upcoming}}
                        <div>
                            <button disabled type="button" class="btn btn-lg chore-buttons" data-toggle="modal" data-target="#myModal">
                                {{chorename}}</br>{{duedate}}</br>({{../firstName}})
                            </button>
                        </div>
                        {{/each}}
                    {{/each}}
                </div>
            </div>
        </div>

        <h4 class="time-titles">Completed</h4>
        <div class="row">
            <div class="col-md-8 col-md-offset-2 col-xs-12 carousel-container">
                <div class="carousel">
                    {{#each users}}
                        {{#each completed}}
                        <div>
                            <button disabled type="button" class="btn btn-lg chore-buttons" data-toggle="modal" data-target="#myModal">
                                {{chorename}}</br>{{duedate}}</br>({{../firstName}})
                            </button>
                        </div>
                        {{/each}}
                    {{/each}}
                </div>
            </div>
        </div>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <script src="js/home.js"></script>
    <script type="text/javascript" src="slick/slick.min.js"></script>
</div>

<!-- Modal -->
<div class="modal fade text-center" id="myModal" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Your Roommate's Chore</h4>
            </div>
            <div class="modal-body">
                <button type="button" class="btn btn-default" data-dismiss="modal">I did it for them!</button>
            </div>
        </div>

    </div>
</div>

</body>
</html>
